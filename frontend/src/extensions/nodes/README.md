Place optional local-only node components here.

Expected layout:

- `frontend/src/extensions/nodes/<category>/<NodeName>.vue`
- `frontend/src/extensions/nodes/<category>/<NodeName>.js`

If a local node is added, register its metadata in `frontend/src/config/localNodeManifest.js`.
