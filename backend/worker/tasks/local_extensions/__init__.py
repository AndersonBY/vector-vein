"""Optional local-only Celery task namespace.

Drop additional desktop-specific task modules in this package to extend the
open-source desktop build without modifying the core worker package tree.
"""
